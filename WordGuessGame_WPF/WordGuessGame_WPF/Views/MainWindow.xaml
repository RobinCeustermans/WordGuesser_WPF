<Window x:Class="WordGuessGame_WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WordGuesser" Height="450" Width="800">
    <Grid>
        <!-- Define the rows and columns -->
        <Grid.RowDefinitions>
            <RowDefinition Height="100" />
            <RowDefinition Height="350" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Place some example elements in the grid -->
        <TextBlock Text="Welcome, play a game or check the high scores" Grid.Row="0" Grid.Column="1" 
                VerticalAlignment="Center" HorizontalAlignment="Center"/>

        <Button x:Name="PlayGame" Content="Play game" Grid.Row="1" Grid.Column="0" Command="{Binding}" CommandParameter="OpenGameConfiguration"
             HorizontalAlignment="Center" VerticalAlignment="Center" Width="100px" />


        <Button x:Name="CheckHighscore" Content="Check highscores" Grid.Row="1" Grid.Column="2" Command="{Binding}" CommandParameter="OpenOverviewPage"
             HorizontalAlignment="Center" VerticalAlignment="Center" Width="100px" />
    </Grid>
</Window>